{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File (reverb) with Arguments",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/feature_extraction/ReverbOptimizer.py",
            "console": "integratedTerminal",
            "args": [
                "--audio-file",
                "squareish_loud_high_clean_synthetic_annoying.wav",
                "--display"
            ],
            "cwd": "${workspaceFolder}/src/feature_extraction"
        }
    ]
}